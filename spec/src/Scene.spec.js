define('spec/Scene.spec', ['argos/Scene'], function(Scene) {
    return describe('argos.Scene', function() {
        it('Can adjust tiers', function() {
            expect(true).toEqual(true);
        });
    });
});
